<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unionpay.qrcode.activity">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SGNetsQrcodeParaActivity" />
        <activity android:name=".NetsQrcodeShowActivity" />
        <activity android:name=".NetsQrcodeParaActivity" />
        <activity android:name=".UnionPayQRcodeActivity" />
        <activity android:name=".EMVConsumerShowActivity" />
        <activity android:name=".EMVConsumerParaActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EMVMerchantParaActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EMVMerchantShowActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>